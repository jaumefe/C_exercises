#include <stdio.h>

int main(int argc, char* argv[]){
    int imin, xmin, num;
    printf("Input the length of the array: \n");
    scanf("%d",num);
    int x[num];
    for(int i = 0; i < sizeof(x)/sizeof(int); i++){
        
    }
}