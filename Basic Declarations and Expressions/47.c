#include <stdio.h>
#include <math.h>

int main(int argc, char* argv[]){
    int num;
    printf("Input an integer:");
    scanf("%d", &num);
    printf("\n");
    int sqrt_div = sqrt(num);
    for(int i = 0; i < sqrt_div ; i++){

    }
}